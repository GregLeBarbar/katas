/**
 * Votre mission : Réaliser la méthode chop()
 *
 * Implémentation simple d'un algorithme de recherche
 * binaire (coup binaire) en JavaScript.
 *
 * Exemple d'utilisation
 * let tableau = [1, 3, 5, 7, 9];
 * let element = 7
 * La fonction chop retournera 3
 *
 * @param {number} element
 * @param {array<number>} tableau
 *
 * @returns {number} L'index de l'élément dans le tableau, ou -1
 * si l'élément n'est pas trouvé.
 *
 * A noter : Le nom de la fonction "chop" signifie "hacher" c'est à
 * dire diviser de manière répétitive en 2 parties égales.
 */

export ...
